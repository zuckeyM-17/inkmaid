# ===========================================
# Inkmaid 環境設定ファイル
# ===========================================
# このファイルをコピーして .env を作成してください
# git worktree を使用する場合は scripts/setup-worktree.sh を実行してください

# -----------------------------------------
# Docker Compose 設定
# -----------------------------------------
# プロジェクト名（コンテナ名のプレフィックスに使用）
# worktree ごとに一意な名前を設定
COMPOSE_PROJECT_NAME=inkmaid

# PostgreSQL ホストポート（デフォルト: 5432）
# worktree ごとに異なるポートを設定
DB_PORT=5432

# Next.js 開発サーバーのポート（デフォルト: 3000）
# worktree ごとに異なるポートを設定
NEXT_PORT=3000

# -----------------------------------------
# PostgreSQL 設定
# -----------------------------------------
POSTGRES_USER=inkmaid
POSTGRES_PASSWORD=inkmaid_password
POSTGRES_DB=inkmaid

# -----------------------------------------
# アプリケーション設定
# -----------------------------------------
# DATABASE_URL は apps/web/.env.local に設定してください
# DATABASE_URL=postgresql://inkmaid:inkmaid_password@localhost:5432/inkmaid

# -----------------------------------------
# Langfuse 設定
# -----------------------------------------
# Langfuse サーバーポート（デフォルト: 3001）
LANGFUSE_PORT=3001

# Langfuse PostgreSQL ポート（デフォルト: 5433）
LANGFUSE_DB_PORT=5433

# Langfuse 認証用シークレット（本番環境では必ず変更）
LANGFUSE_NEXTAUTH_SECRET=langfuse-local-dev-secret-change-in-production
LANGFUSE_SALT=langfuse-local-dev-salt-change-in-production
# 暗号化キー（必須）: openssl rand -hex 32 で生成
LANGFUSE_ENCRYPTION_KEY=

# -----------------------------------------
# Langfuse SDK 設定 (apps/web/.env.local に設定)
# -----------------------------------------
# ローカル開発時
# LANGFUSE_HOST=http://localhost:3001
# LANGFUSE_PUBLIC_KEY=pk-xxx  # Langfuse UIで作成
# LANGFUSE_SECRET_KEY=sk-xxx  # Langfuse UIで作成
#
# SaaS利用時（本番環境）
# LANGFUSE_HOST=https://cloud.langfuse.com
# LANGFUSE_PUBLIC_KEY=pk-xxx
# LANGFUSE_SECRET_KEY=sk-xxx


